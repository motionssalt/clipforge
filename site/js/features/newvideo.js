/** task-03 will implement the full new-video wizard here. */
import { escapeHtml } from '../state.js';
export async function renderNewVideo(app) {
  app.innerHTML = `<div class="card"><h2>New video</h2><p class="muted">Under construction — see site/BUILD_STATE.json task-03.</p></div>`;
}
