{
  "sweep_complete": false,
  "bugs": [
    {
      "id": "bug-66",
      "title": "Cleanup deleted ALL jobs, not a small/unlucky subset - series_is_complete() ignores the is_final flag, wiping mid-series jobs",
      "status": "done",
      "notes": "ROOT CAUSE RE-VERIFIED against current repo (not trusted blindly): cleanup commit 1d6ad63 (2026-08-29 21:43:52 UTC per its commit timestamp 1788027832) deleted jobs manual-1787970449255, manual-1787978239962, series-1787970477573-p2; their status.json at parent dce3b5f shows expires_at_epoch genuinely past (10581s/3368s/7475s respectively) and publishing.status=scheduled with post times after deletion — active, unfinished work. Defect confirmed in code: read_job_timing() extracted series_final but series_is_complete() never consumed it; a series whose existing parts were all terminal counted complete with ZERO is_final parts. Sibling comparison confirmed: series-1787978275321-p2 (is_final:true) still on disk — its p1 reap was legitimate; series-1787970477573 had p1+p2 both is_final:false (part 3 never rendered) — both wrongfully wiped. FIX (commit e81ac72): series_is_complete() now requires (a) every on-disk part terminal (unchanged bug-49 rule: non-terminal/unreadable sibling still protects) AND (b) >=1 part marked is_final; zero-final series = incomplete. No other behavior in expired.py touched (expiry fallback chain, 3-phase main, release/branch handling intact). TESTS: fixed 3 stale JobTiming assertions to current return shape (series_id/series_final), added test_reads_series_fields + 4 bug-66 regression cases incl. full mocked end-to-end replay of the incident (mid-series protected; final-marked series still reaped). test_cleanup.py 26/26 pass; full pipeline suite 305 passed with only the 10 pre-existing unrelated failures (test_stage_a x2, test_stage_b x7, test_status x1) that fail identically on unmodified main — zero new failures. RECOVERY (commit 0e2f9d7, done AFTER the fix was verifiably live on origin/main): restored all 16 files of the 3 job folders byte-identical from parent commit dce3b5f, unmodified — no TTL extensions; jobs whose expires_at_epoch has since genuinely passed will expire normally under the fixed logic. PERMANENTLY LOST (honest scope): all three jobs had reached state=complete with rendered assets — their GitHub Releases, final.mp4 / final.zip assets, clipforge-<job-id> git tags, and per-job branches were destroyed via irreversible GitHub API DELETE calls in the 1d6ad63 run and CANNOT be recovered by any means; all three require a full re-render to get the finished videos back. Restored metadata (stage-a-request, production plan, ingest, torrent-selection, status incl. scheduled post details) tells the operator exactly what to re-render. PUSH VERIFIED: all session commits confirmed on origin/main via git fetch + git log origin/main (0a7dcd1 seed/push-test, aab8c4c in_progress, e81ac72 fix, 0e2f9d7 recovery, plus this final state update).",
      "commit": "e81ac72"
    },
    {
      "id": "bug-67",
      "title": "scripts/reclaim-stale-task-labels.mjs reclaims an active, non-final series part's label the moment its own status goes terminal, because the terminal-state check runs BEFORE the series-incomplete check (classify() ordering bug; seriesIncomplete() never reached for terminal parts)",
      "status": "in_progress",
      "notes": "ROOT CAUSE RE-VERIFIED against current code (not trusted from prompt): scripts/reclaim-stale-task-labels.mjs classify() returned `stale:terminal-<state>` (old line 108) BEFORE the bug-49 series-protection branch (old lines 109-112), making that protection unreachable dead code for terminal series parts — the exact manual-1788023189426 incident shape. SECOND DEFECT CONFIRMED: this script's seriesIncomplete() only checked for non-terminal siblings and did NOT require a terminal is_final part — the same gap bug-66 fixed in pipeline/cleanup/expired.py's series_is_complete() (which now requires every on-disk part terminal AND >=1 part with series_final/is_final). Both defects present. FIX IMPLEMENTED: (1) extracted decision logic to NEW scripts/reclaim-stale-task-labels-lib.mjs exporting classify() + makeSeriesIncomplete() (same pattern as bug-62's jsonc.mjs extraction) so it is unit-testable without the CLI's top-level env/KV/wrangler side effects; (2) classify() now runs the series-membership/incomplete guard BEFORE the terminal short-circuit; (3) makeSeriesIncomplete() enforces the bug-66 is_final rule (zero-final series = incomplete) and skips unreadable sibling statuses; (4) reclaim-stale-task-labels.mjs slimmed to import the lib, wiring its GitHub-jobs listing as the lister. TESTS: NEW bot/test/reclaim-stale-task-labels.test.mjs, 13 cases ALL PASS — incident replay (terminal part-1 is_final=false no sibling -> active), pre-fix ordering anchor, terminal+non-terminal sibling -> active (bug-49), terminal is_final sibling -> stale:terminal-complete, bug-66 zero-final all-terminal series -> active, non-series terminal -> stale (no regression), series disabled -> stale, active future-TTL -> active, past-TTL -> stale:ttl-expired, missing/unparseable -> stale:no-readable-status, http/no-token -> unknown, unreadable sibling skipped (matches expired.py), cache behavior, lister-failure fallback. REGRESSION CHECK: full bot/test suite — all previously-passing files still pass (animation 10, jobs 10, plan 5, series 11, wrangler-jsonc 5, zernio 28, reclaim 13 = 82 passed); the 6 failing files (identity, clone-jobs, narrator-previews, relay, tasks, wizard) fail IDENTICALLY on pristine main via git stash (missing tweetnacl dependency — pre-existing environment issue, unrelated). CLI SMOKE: node --check passes both files; no-REPO run exits 2 with usage error; REPO=motionssalt/clipforge run loads module graph + parses wrangler.bot-a.jsonc and reaches the wrangler KV step (fails there only for missing CLOUDFLARE credentials — expected in this sandbox; dry-run against live KV not possible without CF API token, noted as environment limit, hourly cleanup.yml retains real credentials). PUSH VERIFIED: seed 74acc25 confirmed on origin/main. REMAINING: none for code; final step is this checkpoint + commit + push + verify, then sweep_complete.",
      "commit": null
    }
  ],
  "completed_at": null
}
