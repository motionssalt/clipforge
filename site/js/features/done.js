/** task-04 will implement the completed list here. */
export async function renderDone(app) {
  app.innerHTML = `<div class="card"><h2>Completed</h2><p class="muted">Under construction — see site/BUILD_STATE.json task-04.</p></div>`;
}
