/** task-04 will implement tasks list + detail + real Actions logs here. */
export async function renderTasks(app) {
  app.innerHTML = `<div class="card"><h2>Tasks</h2><p class="muted">Under construction — see site/BUILD_STATE.json task-04.</p></div>`;
}
export async function renderTaskDetail(app, jobId) {
  app.innerHTML = `<div class="card"><h2>Task</h2><p class="muted">Under construction — task-04.</p></div>`;
}
