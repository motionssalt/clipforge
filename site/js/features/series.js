/** task-05 will implement the Series view here. */
export async function renderSeries(app) {
  app.innerHTML = `<div class="card"><h2>Series</h2><p class="muted">Under construction — see site/BUILD_STATE.json task-05.</p></div>`;
}
