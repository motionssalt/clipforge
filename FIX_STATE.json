{
  "sweep_complete": false,
  "sweep_started": "2026-08-30T00:00:00Z",
  "previous_sweep": {
    "note": "Prior sweep (bug-66, bug-67) marked complete; preserved in git history. New sweep seeded per session contract.",
    "completed_at": "2026-08-29T00:00:00Z",
    "commits": ["e81ac72", "5b4db4c"]
  },
  "bugs": [
    {
      "id": "bug-68",
      "title": "analysis_720p.mp4 is generated and uploaded to every Stage A release but never read downstream — stop generating/uploading it",
      "status": "pending",
      "notes": "Seeded 2026-08-30. Plan: (1) verify via grep that analysis_720p.mp4 is referenced ONLY in pipeline/stage_a/bundle.py and .github/workflows/stage-a.yml; (2) remove the 720p transcode step from stage-a.yml while preserving any still-load-bearing duration probing; (3) remove it from bundle.py asset list and release body text; (4) update series_reuse block to stop downloading/expecting it and relax the bundle-assembly post-condition check; (5) update tests in pipeline/tests/; (6) run full pipeline test suite.",
      "commit": null
    },
    {
      "id": "bug-69",
      "title": "Stage A releases fail outright when source_input.bin exceeds GitHub's 2 GiB per-asset limit — retry release without source, Stage B re-fetches source from stage-a-request.json",
      "status": "pending",
      "notes": "Seeded 2026-08-30. GATED on bug-68 being done and verified on origin/main. Design is operator-approved: try full upload first (no preemptive size skip); on confirmed 2GiB-limit failure specifically, retry without source_input.bin (idempotent vs partially-succeeded first attempt); record intentional omission in release body/manifest; Stage B falls back to re-fetch via pipeline/stage_a/ingest.py existing resolution logic using stage-a-request.json's source object (incl. exact torrent_file_index for torrents, no interactive re-prompt); series_reuse must also fall through to Part 1's original source reference when source was omitted; verify stage-a-request.json persists all fields needed per source kind; test normal path, failure+retry path, Stage B re-fetch (torrent + direct URL), and series interaction.",
      "commit": null
    }
  ]
}
