/** task-02 will implement Shadow Clone creation here. */
export async function renderCloneCreation(app) {
  app.innerHTML = `<div class="card"><h2>Create Shadow Clone</h2><p class="muted">Under construction — see site/BUILD_STATE.json task-02.</p></div>`;
}
