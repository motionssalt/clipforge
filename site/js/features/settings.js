/** task-07 will implement all Settings sections here. */
export async function renderSettings(app) {
  app.innerHTML = `<div class="card"><h2>Settings</h2><p class="muted">Under construction — see site/BUILD_STATE.json task-07.</p></div>`;
}
