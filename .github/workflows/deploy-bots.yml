name: Deploy Bots — Cloudflare Workers

# ARCHITECTURE.md §12 (preserved behavior): on every bot/** change, run the
# bot test suite and then deploy Bot A and Bot B Workers. Ported from
# _legacy/.github/workflows/deploy-telegram-workers.yml with the layout
# updated: the workers now live in bot/ (npm + package-lock.json, Node >=20,
# ESM tests via `npm test`), and the two wrangler configs are
# bot/wrangler.bot-a.jsonc / bot/wrangler.bot-b.jsonc.
#
# Secrets are referenced by name only (§13 invariant #1):
#   CLOUDFLARE_API_TOKEN   — Cloudflare API token with Workers edit rights
#   CLOUDFLARE_ACCOUNT_ID  — Cloudflare account id
# Worker runtime secrets (TELEGRAM_BOT_TOKEN, KV_ENCRYPTION_KEY, …) are NOT
# set here — they are managed with `wrangler secret put` per the comments in
# each wrangler config and are never touched by CI.
#
# bug-58: a failed test step used to silently skip the wrangler deploys — the
# only "alert" was the Actions UI, so blocked deploys were discovered by the
# user noticing a landed fix still misbehaving live (see bug-56). The final
# step below (`if: failure()`) now pages the owner on Telegram whenever ANY
# step fails. It uses Bot B's token (BOTB_MTPROTO_BOT_TOKEN, already an
# Actions secret on this central repo for telegram-relay.yml) and posts to
# DEPLOY_ALERT_CHAT_ID when that secret is set, falling back to the internal
# relay group (-5405387856, same non-secret id as INTERNAL_RELAY_GROUP_CHAT_ID
# in bot/wrangler.bot-a.jsonc — Bot B is a member there). No Cloudflare-scoped
# secret is touched by the alert path. The alert itself is best-effort: it
# must never mask the original failure.
#
# bug-68: BOTB_MTPROTO_BOT_TOKEN exists ONLY on this central repo (the relay
# subsystem is central-only, ARCHITECTURE.md §9.2), so on every Shadow Clone
# the alert step below used to log a warning and exit 0 — the clone owner was
# never told their deploy was blocked. Clones have no bot token of their own
# (§10: one shared Bot A serves all users), so the durable fix is bot-side:
# the shared Bot A polls the CONNECTED repo's Deploy Bots runs with the
# owner's PAT on the home-screen touchpoint and DMs the owner on a failure
# (see announceDeployFailure in bot/src/commands/start.js). This workflow-side
# step remains the real-time alert for the main account; on a clone it now
# fails loudly (after logging the pointer) instead of silently succeeding, so
# a missing-secret clone run stays visibly failed in the Actions UI rather
# than looking like a clean skip.

on:
  push:
    branches: [main]
    paths:
      - 'bot/**'
      - '.github/workflows/deploy-bots.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-bots
  cancel-in-progress: false

jobs:
  deploy:
    name: Test and deploy Bot A + Bot B
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: bot
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: bot/package-lock.json

      - name: Install bot dependencies
        run: npm ci --ignore-scripts

      - name: Run bot test suite
        run: npm test

      - name: Deploy Bot A Worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: bot
          command: deploy --config wrangler.bot-a.jsonc

      - name: Deploy Bot B relay Worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: bot
          command: deploy --config wrangler.bot-b.jsonc

      - name: Alert owner on Telegram when the deploy is blocked
        # bug-58: fires on ANY earlier step failure — bot tests failing (the
        # documented silent-skip case), npm ci, or a wrangler deploy itself.
        # Best-effort and self-skipping when the token secret is absent, so it
        # can never turn a failed run into a differently-failed run.
        if: ${{ failure() }}
        working-directory: .
        shell: bash
        env:
          ALERT_BOT_TOKEN: ${{ secrets.BOTB_MTPROTO_BOT_TOKEN }}
          ALERT_CHAT_ID: ${{ secrets.DEPLOY_ALERT_CHAT_ID }}
        run: |
          set -uo pipefail
          if [ -z "${ALERT_BOT_TOKEN:-}" ]; then
            # bug-68: this is a Shadow Clone (BOTB_MTPROTO_BOT_TOKEN exists
            # only on the central repo). The owner is notified by the shared
            # Bot A polling this repo's failed Deploy Bots runs
            # (bot/src/commands/start.js#announceDeployFailure) — but do NOT
            # exit 0 here: a green "alert" step on a failed clone run reads
            # as if the alert went out. Fail loudly so the run stays visibly
            # red in the Actions UI; the original failure is already recorded
            # by the failing step, and this step runs only under failure().
            echo "BOTB_MTPROTO_BOT_TOKEN secret not available on this repo (Shadow Clone): no workflow-side Telegram alert possible." >&2
            echo "The clone owner is notified via the shared ClipForge bot's deploy-failure poll (bug-68)." >&2
            exit 1
          fi
          chat_id="${ALERT_CHAT_ID:-}"
          if [ -z "$chat_id" ]; then
            # Internal relay group — same id as INTERNAL_RELAY_GROUP_CHAT_ID in
            # bot/wrangler.bot-a.jsonc (a public var, not a secret).
            chat_id="-5405387856"
          fi
          short_sha="${GITHUB_SHA:0:7}"
          run_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          text="⚠️ Deploy blocked: a Deploy Bots step failed on commit ${short_sha}; the Cloudflare Workers were NOT fully updated. Check the Actions run: ${run_url}"
          payload=$(python3 -c 'import json,sys; print(json.dumps({"chat_id": sys.argv[1], "text": sys.argv[2], "disable_web_page_preview": True}))' "$chat_id" "$text")
          if curl --fail --silent --show-error --max-time 20 \
              -H 'Content-Type: application/json' -d "$payload" \
              "https://api.telegram.org/bot${ALERT_BOT_TOKEN}/sendMessage" >/dev/null; then
            echo "Deploy-failure alert sent to Telegram chat ${chat_id}."
          else
            echo "WARN: Telegram deploy-failure alert could not be delivered." >&2
          fi
          # Never fail (or change the outcome of) the workflow from here.
          exit 0
